#INTIALIZE ARRAY
arr=[1,2,3,4,5];
sum=0;
#loop through the array to calculate sum of elements
for i in range (0,len(arr)):
    sum=sum+arr[i]
 print=("sum of all the elementsof an array=",sum)
